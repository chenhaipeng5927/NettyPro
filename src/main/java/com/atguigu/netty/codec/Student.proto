syntax = "proto3";//版本
option java_outer_classname = "StudentPOJO";//生成的外部类名 同时也是文件名
message Student { //会在StudentPOJO 外部类生产一个内部类 Student 是真正发送的pojo对象
    int32 id = 1; //Student类中有一个属性 名字为id 类型为int32（proto32） 1表示属性序号 不是值
    string name = 2;
}
